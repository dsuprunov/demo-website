name: demo-portal

image:
  repository: dsuprunov/demo-portal
  tag: latest
  pullPolicy: Always

service:
  type: NodePort
  port: 3000

ingress:
  enabled: false

env:
  - name: PORT
    value: "3000"
